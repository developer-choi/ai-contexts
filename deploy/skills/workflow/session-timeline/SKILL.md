---
description: 세션 로그(jsonl)에서 날짜·시각이 붙은 대화 타임라인 md를 뽑는다. 내부에서 증거로 쓰는 회고용 모드와 채용과제 대화내역 제출용 모드가 있다. 호출형 — 호출 컨텍스트가 안내·호출한다.
---

# 세션 타임라인 추출

## 목적

세션 로그에서 사용자·AI 발화를 날짜+시각과 함께 뽑아 시간대별로 끊은 대화 타임라인 md 한 편을 만든다.

## 모드

산출물이 어디로 나가느냐에 따라 사용자 발화를 다르게 다룬다. 두 모드는 대등하다 — 한쪽이 다른 쪽의 예외가 아니다.

| 모드 | 산출물이 나가는 곳 |
|---|---|
| 회고용 | 내부. 그 세션이 자기 자신을 돌아볼 때 증거로 삼는다 |
| 제출용 | 외부. 채용과제가 AI 세션 대화 내역 제출을 요구할 때 낸다 |

시작 전에 어느 모드인지 사용자에게 확인한다. 모드가 갈리는 지점은 「사용자 발화 처리」뿐이고, 나머지 절차는 같다.

## 절차

1. **대상 세션 jsonl 확정** — `node {{skill_dir}}/session-timeline/scripts/extract-timeline.mjs --list <cwd 경로>`. 세션마다 시작·종료·소요·블록 수·첫 발화가 나온다. 그중 어느 세션인지를 사용자와 확정한다 — 후보가 여럿일 때 고르는 것이 판단이고, 잘못 고르면 타임라인 한 편을 다 뽑고 나서야 드러난다
2. **추출** — `node {{skill_dir}}/session-timeline/scripts/extract-timeline.mjs <세션.jsonl> --out <타임라인.md>`. 평면 타임라인이 나온다
3. **AI 장문 요약 치환** — `node {{skill_dir}}/session-timeline/scripts/inspect-blocks.mjs <타임라인.md> --over 150`으로 치환 대상을 받아, 각 블록을 서브에이전트로 `[요약]` 1~3줄로 줄인다(기준은 「공통 정제 규칙」). 끝나면 `--against <치환 전.md>`로 대조한다 — 다른 블록이 사라지거나 순서가 바뀌었으면 여기서 걸린다. **이 대조를 건너뛰면 줄어든 판본이 다음 단계의 기준선이 되어 유실이 검사를 통과한다**
4. **시간대별 헤딩 삽입** — 타임라인을 활동 구간으로 나누고(「시간대별 헤딩 양식」), 구간마다 **시작 시각과 한 일 요약만** json으로 적어 `node {{skill_dir}}/session-timeline/scripts/insert-headings.mjs <타임라인.md> --segs <구간.json> --out <최종.md> [--note "집계 한 줄"]`. 끝시각·소요분·헤딩·상단 조망·합계는 스크립트가 만든다. 시작 시각 재료는 `inspect-blocks.mjs --stamps`가 낸다 — md를 통독해 옮겨 적지 않는다
5. **사용자 발화 처리** — 「사용자 발화 처리」의 해당 모드를 적용한다
6. **출력**: 대상 프로젝트 `plan/retrospective/[세션이름].md` — 세션 이름은 영문 슬러그(소문자 + 하이픈)

로그 포맷의 함정(메타 주입·도구 반환·래퍼 태그·재전송·쪼개진 턴 등)은 `extract-timeline.mjs`가 단일 출처다. 산출물에 그중 하나가 새어나오면 스킬 본문에 예외를 적지 말고 그 스크립트를 고친다.

## 공통 정제 규칙

- 화자 라벨: **사용자 / AI / 시스템**
- 사용자의 긴 붙여넣기는 `[한 줄 요약]`으로 대체
- **AI 발화 150자 초과 → `[요약]` 1~3줄** — 핵심 주장·결정·제안·질문을 담는다("설명했다" 금지). 이하는 전문 유지. 치환했으면 상단 메타 인용 블록에 그 사실을 한 줄로 밝힌다

## 사용자 발화 처리

### 회고용 모드

사용자 발화를 원문 그대로 둔다 — 요약·삭제·재해석하지 않는다. 승인성 입력("ㅇㅋ")도 남긴다. 회고는 자기 실수를 감싸려는 편향을 기록으로 반박하는 자리라 원문이 아니면 증거가 되지 못하고, 승인 횟수 자체도 지표다.

### 제출용 모드

사용자 발화의 **어투만** 순화한다. 외부로 나가는 문서라 읽는 쪽이 어투에 걸리면 내용이 전달되지 않는다.

- 욕설·비속어·과격한 표현은 삭제하거나 중립 표현으로 바꾼다. 발화 전체가 욕설이면 그 발화를 삭제한다
- 반말·명령조 등 무례하게 읽힐 수 있는 어투는 존댓말 또는 중립 표현으로 바꾼다
- 요구·결정·질문 등 내용은 삭제·왜곡하지 않는다. 어투를 걷어내면 내용이 사라지는 발화만 통삭제한다
- AI·시스템 블록은 손대지 않는다. 순화를 마치면 `inspect-blocks.mjs --against <순화 전.md>`로 대조한다 — **이 산출물은 외부로 나가고, AI 블록이 슬쩍 편집된 대화록이 제출되면 회수할 수단이 없다**
- 정제 후 사용자에게 결과 검토를 받는다 — 외부 제출물이라 사용자 최종 확인이 있어야 닫힌다. 다만 사용자가 보는 것은 어투이지 블록 대조가 아니므로, 위 대조를 그 앞에 둔다

## 시간대별 헤딩 양식

문서는 두 층이다 — 상단 조망(`# 시간대별 요약본`)과 하단 대화. 둘 다 같은 구간 배열에서 스크립트가 만든다.

존재 이유: 순서대로 읽을 땐 안 보이지만 구간으로 모아 보면 "이걸 이 시간만큼 써서 했다고?"가 드러난다. 그래서 **소요분이 조용히 틀리면 회고 결론이 그대로 오염된다** — 그 산술을 손으로 안 하는 이유다.

정하는 것은 구간을 어디서 끊을지와 한 일 요약뿐이다.

- 구간은 활동 단위로 나눈다 (단계 진입 / 실행 / 리뷰 / 재작업 / 사고 대응 / 세션 재개 등)
- **판단(의사결정) 단위로 묶는다** — 표면 주제가 여럿이어도(코드 붙여넣기·문서 읽기·라이브러리 조사 등) 그것들이 **하나의 판단을 위한 탐색**이었으면 그 판단 구간으로 합친다. 목적은 "그 판단에 실제로 몇 분 썼나"를 end-to-end로 드러내는 것 — 표면 활동으로 쪼개면 판단 비용이 '코드리뷰'·'문서읽기'로 흩어져 안 보인다. (실증 PR_7_PLAN: 붙여넣기 try/catch 반려·step5.md·MP ApiClient 조사가 모두 "에러처리를 어떤 구조로 공통화할까" 한 판단의 탐색 → 24분 탐색 + 11분 확정을 한 arc로 묶어 판단 35분을 가시화.)
- 요약엔 핵심 결정·커밋 해시·등록한 task를 보존한다
- 합계 줄의 **총 소요는 스크립트가 낸다.** 거기 덧붙일 「눈에 띄는 집계 1개」만 `--note`로 넘긴다 (예: "첫 커밋까지 98분, 교정 루프 83분 — 교정이 최초 작업과 맞먹음")

## 주의

- [CRITICAL] **사용자가 의도적으로 잘라낸 구간과 스크립트 유실을 구분한다.** 사용자 삭제면 존중하고 되살리지 않는다. (사용자가 뺀 메타 구간을 유실 사고로 오인해 재추출로 되살린 오판 사례 있음 — 산출물은 사용자가 남기려는 실작업 구간만 유지한다.)
