# Step 1: 배경 파악 및 문제 정의

> **이 단계의 목표: 작업의 문제/목표를 정의한다** (도메인/비즈니스 관점, 코드 분석 아님)

작업의 **배경**, **목적**, **문제가 있다면 Root Cause**를 명확히 합니다.

---

## 스킬 로드

아래 플래그를 조건에 맞게 켜서 [requirement-review](../requirement-review/SKILL.md)를 호출한다. 여러 개를 동시에 켤 수 있다.

| 플래그 | 켜는 조건 | 실행 | 설명 |
|--------|-----------|------|------|
| `recruitment` | 채용 과제 | 메인 | 과제 요구사항 리뷰 |
| `planning` | 신규 기능 / 리팩토링 / 채용과제에 페이지 단위 기획이 있음 (UI 목업, 기능 명세 포함) | 메인 | 기획서·피그마·이슈 기반 배경 파악 |
| `design` | 디자인 시안(Figma 등)이 별도로 있음 | 메인 | 디자인 시안 리뷰 |

배경 산출물은 `/plan/background/`에, 페이지별 산출물은 `/plan/pr{N}/`에 생성됩니다.

---

## 중복 조기 발견

requirement-review 단계에서 디자인·컴포넌트 중복이 있으면 선제적으로 짚는다. 나중에 통합 얘기를 꺼내면 중복 QA가 발생하므로, 초기에 발견하는 것이 비용이 적다.

---

## 추가 맥락 수집

스킬 실행 전후로, 사용자에게 추가 맥락이 있는지 확인합니다:

> "추가로 참고할 자료가 있나요? (예: 사내 컨벤션 가이드, 기획자/디자이너 전달사항, 기존 기술 스택 제약 등)"

있다면 내용을 요약하여 적절한 파일명으로 `/plan/background/`에 저장합니다. (예: `background/team-conventions.md`, `background/designer-notes.md`)

---

## 작업 익숙도 판별

사용자에게 이번 작업이 익숙한 영역인지 확인한다:

> "이번 작업에서 처음 다루는 기술이나 낯선 영역이 있나요?"

낯선 작업이라면:

1. 공부 세션을 위한 인수인계 문서를 작성한다:
   - 이 기술이 왜 필요한지 (작업 배경, 해결하려는 문제)
   - 알아야 하는 키워드, 핵심 API 등을 학습 순서대로 나열
   - 구체적인 학습 방법(how)은 포함하지 않는다 — 공부 세션에서 AI와 함께 결정
2. **별도 세션에서 인수인계 문서 기반으로 병행 학습**하면서, 이 세션에서는 기획을 계속 진행하도록 안내한다
3. 이 세션에서는 사용자가 방향 잡기(라이브러리 선택, 참조할 원본 코드 지정 등)에 집중하고, 구현 판단은 AI가 맡되 리뷰를 더 꼼꼼히 받는 구조로 진행
