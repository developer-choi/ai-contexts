# Step 1: 배경 파악 및 문제 정의

> **이 단계의 목표: 작업의 문제/목표를 정의한다** (도메인/비즈니스 관점, 코드 분석 아님)

BG 세션은 **결과물 스펙(WHAT)** 을 분석한다 — 동작·기획·디자인. "무한스크롤이 필요하다"까지가 BG다. 그것을 Intersection Observer로 짤지 Scroll Event로 짤지 같은 **개발 방법론(HOW)** 은 PLAN/IMPL 소관이다. 산출물 깊이는 이 축으로 잡는다([output-depth.md](../template/output-depth.md)).

step-1은 BG 세션 안에서 두 sub-step으로 진행된다. 1.1은 자료 입수 단계, 1.2는 requirement-review 본체.

- **Step 1.1 자료 받기**: 사용자에게서 자료를 받아 정리. 자료 입수 완료 시점이 후속 세션(FOUNDATION·MARKUP)의 spawn 가능 조건이다 (SKILL.md 「세션」 표 참조)
- **Step 1.2 requirement-review 본체**: 스킬 로드 + 중복 조기 발견 + 작업 익숙도 판별

**PR 확정은 별도 step이 아니라 step-1 내내 굴러간다** — 자료를 읽어나가며 확정 가능한 PR부터 하나씩 `project.md`에 append한다 (아래 「PR 확정」). BG는 step-1로 끝난다; 일괄 PR 분할 단계는 없다.

---

## Step 1.1 자료 받기

### 자료 수집 점검

아래 스킬 로드 표에서 `planning` 또는 `design` 플래그가 켜지는 작업, 즉 외부에서 시안·기획서·Figma 자료를 받는 케이스라면, 호출 전에 자료 수집 방식이 적절한지 점검한다. 상세는 [design-asset-collection.md](../template/design-asset-collection.md). 자료를 아직 받기 전이면 능동 안내한다.

채용 모드인 경우 사용자가 제공한 공고·메일·과제요구사항을 `/plan/background/persistent/`에 저장한다 (requirement-review/recruitment/guide.md 「입력 자료」 참조). 저장 시 시간 압박 표현 제외 (guide.md 「입력 자료 저장 시 제외 항목」).

**step-1.1은 자료의 단일 수집 지점 — 가진 것 중 가장 완성도 높은 원본(SSOT)을 여기서 받아 저장한다.** 원본의 형태는 모드마다 다르지만(채용=figma+과제요구사항, 실무=figma+기획서, 개인=마크업 시안+선택 spec.md), **시각 진실 원천을 step-1.1에 받아 retained에 보존한다**는 점은 모드 무관으로 같다. 사람이 만든 것이라 불완전할 수 있고, 빠진 부분은 step-1.2 리뷰에서 구멍으로 잡는다. figma 쓰는 모드(채용·실무)는 **사용 가능한 figma/시안을 여기서 전부** 받아 저장한다 (figma URL·캡처 → `retained/figma-url.md`·`retained/figma/`). 다운스트림(requirement-review·MARKUP)은 이 수집분을 *참조*하며 자료를 재요청하지 않는다 — 빠진 것이 있으면 그 갭만 콕 집어 보충한다. (UI는 생김새만이 아니라 상태·인터랙션까지 담아 부분적으로 명세 역할을 하므로, design·기획 자료를 분산 수집할 이유가 없다.)

**개인 모드**도 step-1.1에서 원본을 받는다 — figma 대신, 사용자가 워크플로우 전에 아이디에이션으로 만든 **마크업 시안**(형태 무관 — HTML/CSS·`.tsx`+`.scss` 등)이 시각 진실 원천이다(figma 자리를 대체). figma를 받는 것과 대칭이며 `retained/mockup/`에 저장해 보존한다. 화면에 안 담기는 동작(API 응답 형태·에러·로딩·엣지값 등)을 저자가 미리 알고 있으면 `retained/spec.md`로 함께 받는다 — 특례가 아니라 "자료를 더 받는 것"이다. step-1.2는 이 원본을 리뷰해 page.md/global.md(비시각 스펙)를 채우고 리뷰 중 나온 구멍은 project.md TODO로 남긴다 — "정의"가 아니라 "가져온 원본 리뷰"다. 사용자가 이미 가진 참고 자료(디자인시스템 소스 등)도 함께 수집한다.

### 컨벤션 소스 수집 — 이름 스캔 선제안 + conventions-index.md

프로젝트 컨벤션 소스 수집의 **단일 지점**이다. 이후 단계(PR별 과제 정의 등)는 여기서 만든 인덱스를 소비만 하고 재수집·재질문하지 않는다.

1. **이름 스캔 선제안**: 작업 레포가 존재하면 Glob으로 컨벤션 성격의 파일을 **이름만** 스캔한다 — `**/CLAUDE.md`, `**/AGENTS.md`, `docs/**`, `*convention*`, `*ARCHITECTURE*`, `*CONTRIBUTING*` 류. 후보 목록을 사용자에게 선제안한 뒤 채택받는다. **이 단계에서 파일 내용은 읽지 않는다** — 채택된 파일도 본문은 그것을 소비하는 단계가 필요할 때 읽는다 (컨텍스트 오염 방지).
2. **사용자 보충**: 선제안 후 추가 맥락을 확인한다:
   > "추가로 참고할 자료가 있나요? (예: 사내 컨벤션 가이드, 기획자/디자이너 전달사항, 기존 기술 스택 제약 등)"
   사용자 제공 원문 자료는 `/plan/background/retained/`에 파일로 저장하고 인덱스에 등재한다.
3. **인덱스 작성**: 채택된 소스를 `/plan/background/retained/conventions-index.md`에 기록한다. 양식은 항목당 **"경로 + 한 줄 트리거(어떤 작업·디렉터리에서 봐야 하는지)"**. 본문 요약은 적지 않는다 — 본문이 바뀌면 stale해지고, 열어보면 어차피 보인다.
   - **개인·실무 모드 표준 참고처 — 관련성 게이트**: `~/WebstormProjects/simplify/` 하위 simplified 레포(MUI·radix 등 라이브러리 원본 미러)는 **이번 작업이 그 라이브러리 컴포넌트에 대응하는 컴포넌트·마크업을 직접 작성하는 경우에만** 표준 참고처로 등재한다. 트리거: "대응 컴포넌트를 이 레포들에서 뒤져 참고". 폼 핸들링 동작·데이터페칭 로직처럼 **컴포넌트 미러링이 없는 동작·로직 중심 작업이면 등재하지 않는다 — 무관한 참고처다.** 등재할 때는 simplify 하위에서 **실제 존재하는 구체 레포 경로를 직접 확인해 나열**한다(막연한 "simplify 하위" 표기 금지). 어느 레포에 어떤 컴포넌트가 있는지의 정확 매핑까지는 박지 않는다 — 그건 자료를 소비하는 세션이 탐색한다. 채용 모드는 제외.
4. **레포가 아직 없으면**(채용 모드에서 starter 미확보 등) 스캔·인덱스 생성을 레포가 확보되는 시점으로 미룬다. 그 시점을 담당하는 세션이 본 절의 절차를 수행한다.

### Step 1.1 종료 — 분기점

step-1.1 종료 = `/plan/background/persistent/`에 원본 자료 저장 완료 + 시각 원본(figma 쓰는 모드는 `retained/figma-url.md`·`retained/figma/`, 개인 모드는 `retained/mockup/`(+선택 `retained/spec.md`)) 저장 완료 + 시각 원본 진입 문서 `retained/design-root.md` 산출(원본 위치 + 대조 절차문서만 적는 한 장 — 양식은 [conventions/artifact/design-root.md](../conventions/artifact/design-root.md)) + 컨벤션 인덱스 산출(아래 종료 게이트). 그 이상(분석)은 모두 step-1.2 영역이다.

특히 다음은 **step-1.2** 영역이지 step-1.1이 아니다 — recruitment guide(`requirement-review/recruitment/guide.md`)·planning guide·design guide의 「분석 과정」 절차 일체가 여기 해당:
- 자료 정독·공동 탐색
- 시안 분석·페이지별 자료 정리·디자인 시스템 설계 (raw 자료 *수집*은 1.1, 1.2는 그 수집분의 정리·분석)
- cross-analysis·service-analysis 등 분석 산출물 작성
- 평가 기준 추론, 프로젝트 결정 사항, 모호한 부분 정리

step-1.1은 후속 세션 spawn 가능 분기점이다 (SKILL.md 「세션」 표 (4) 컬럼). 자료 저장 직후, **분석(step-1.2)이나 구현 설계로 진입하기 전에 두 산출을 먼저 낸다** — ① 위 「컨벤션 소스 수집」 절(이름 스캔 선제안·채택, 사용자 보충, `retained/conventions-index.md` 작성), ② SKILL.md 「세션 spawn 안내 메커니즘」 발동. 이 둘은 다음 행동의 전제다 — 둘 중 하나라도 내지 않은 채 분석·구현 절차를 시작하지 않는다. ①을 건너뛰고 다운스트림(PR별 과제 정의·MARKUP)으로 가면 컨벤션 1차 소스를 거기서 재수집하게 되고, 같은 추출 패턴의 Reviewer가 그 누락을 못 잡는 자기증명 루프가 생긴다(SKILL.md 「검증 기준 = 진실 원천」). 컨벤션 파일을 선제안·채택 없이 혼자 읽고 진행하는 것도 ① 위반이다. 작업 레포가 아직 없어 ①의 인덱스 작성을 미루는 경우(절 4항)는 연기 사실을 한 줄로 남긴다. 두 산출이 끝나 사용자가 후속 세션 spawn을 판단할 수 있게 된 다음에 step-1.2로 진입한다.

---

## Step 1.2 requirement-review 본체

### 스킬 로드

아래 플래그로 [requirement-review](../requirement-review/SKILL.md)를 호출한다. `recruitment`는 세션 모드가 채용이면 자동 ON, 아니면 OFF다(모드에서 결정 — 되묻지 않는다). `planning`·`design`은 표 조건으로 AI가 임의 판정하지 않고, 각 플래그를 켤지 사용자에게 질문한다 — 사용자 답대로 켠다.

| 플래그 | 실행 | 설명 |
|--------|------|------|
| `recruitment` | 메인 | 과제 요구사항 리뷰 |
| `planning` | 메인 | 기획서·피그마·이슈 기반 배경 파악 |
| `design` | 메인 | 디자인 시안 리뷰 |

`planning`·`design` ON/OFF를 물을 때, 리뷰 대상에 목업·시안이 없고 기존 구현 코드만 있으면(기존 동작 코드 개선형 과제 등) 그 사실을 함께 알리고 "코드를 기획·디자인 자료 대용으로 써서 체크리스트를 리뷰할까요?"로 묻는다. 켜면 각 guide.md의 "코드기반 모드" 절차로 진행한다.

산출물을 작성할 때 깊이 기준은 [output-depth.md](../template/output-depth.md)를 따른다. PLAN/IMPL 세션에 미루어야 할 항목(validation 규칙, 함수 시그니처, 구현 task 단위 등)은 BG에서 적지 않는다.

### 중복 조기 발견

requirement-review 단계에서 디자인·컴포넌트 중복이 있으면 **사용자에게 명시 통보**한다. cross-analysis·평가 기준 추론 같은 분석 산출물에 묻어서 흡수만 시키지 않는다 — 산출물에 묻히면 사용자가 통합 결정을 늦게 가져오고 PLAN·IMPL에서 중복 QA가 발생한다.

선제 통보 형식: "시안 카드 N종에서 공통 골격(타이틀·설명·상태 아이콘 등) 발견 → 컴포넌트 추출 대상. 지금 통합 결정 vs 나중에 PLAN으로 미루기 중 선택" 같이 사용자가 결정해야 할 항목 형태로 통보.

### 작업 익숙도 판별

사용자에게 이번 작업이 익숙한 영역인지 확인한다:

> "이번 작업에서 처음 다루는 기술이나 낯선 영역이 있나요?"

**미확정 결정 항목을 전제로 끼워 넣지 않는다**: 폴더 구조 등 아직 사용자 확정을 거치지 않은 프로젝트 결정 사항(guide.md #3 「프로젝트 결정 사항」 참조)을 이미 채택된 것처럼 낯선 영역 예시에 포함하지 않는다. 예시가 필요하면 확정된 항목만 든다.

낯선 작업이라면:

1. 공부 세션을 위한 인수인계 문서를 작성한다:
   - 이 기술이 왜 필요한지 (작업 배경, 해결하려는 문제)
   - 알아야 하는 키워드, 핵심 API 등을 학습 순서대로 나열
   - 구체적인 학습 방법(how)은 포함하지 않는다 — 공부 세션에서 AI와 함께 결정
2. **별도 세션에서 인수인계 문서 기반으로 병행 학습**하면서, 이 세션에서는 기획을 계속 진행하도록 안내한다
3. 이 세션에서는 사용자가 방향 잡기(라이브러리 선택, 참조할 원본 코드 지정 등)에 집중하고, 구현 판단은 AI가 맡되 리뷰를 더 꼼꼼히 받는 구조로 진행

인수인계 문서가 작성되었으면, **PR_{N}_PLAN 진입 안내** 시 "사용자가 인수인계 문서 학습을 완료한 뒤 진입하라" 조건을 함께 안내한다 (SKILL.md 「학습 인수인계 후 세션 진입 대기」 참조).

---

## PR 확정 — step-1 내내

자료를 읽어나가며 **확정 가능한 PR부터 하나씩** `/plan/background/consumable/project.md`에 append한다. 확정 기준·분할 원칙·의존 서술은 [conventions/pr-split.md](../conventions/pr-split.md)가 단일 출처다.

- 모든 PR이 정해질 때까지 기다리지 않는다. 확정된 PR은 곧바로 출발 후보가 된다.
- 확정 기준은 "**다른 자료를 더 안 봐도 확정 가능한가**" — 안 읽은 자료가 그 PR의 경계를 바꿀 수 있으면 확정하지 않는다.
- 확정할 때마다 사용자에게 보고하고(범위 한 줄 + 선행마다 무엇이 있어야 착수 가능한지 + 열린 질문), **그 자리에서** SKILL.md 「세션 spawn 안내 메커니즘」을 발동한 뒤 남은 분석을 이어간다. 확정을 모아뒀다 한꺼번에 안내하지 않는다 — 모으면 일괄 분할과 같아져 대기가 되살아난다.

### 순서 우려는 사용자에게 제안한다

**PR 진행 순서는 BG가 정하지 않는다 — 판단은 사용자 몫이다.** BG는 자료와 레포를 읽고 **그때그때 따져서**, 걸리는 게 보이면 **이유를 붙여 제안**한다. 사용자가 그 제안을 보고 정한다.

무엇이 걸리는지는 프로젝트마다 다르므로 **본 문서에 해당 항목 목록을 두지 않는다.** BG가 실제 자료를 읽고 도출한다.

제안의 모양:

> "이 PR은 출발은 지금 해도 될 것 같습니다. 다만 중간에 ○○는 △△를 기다려야 합니다 — tsconfig 경로 설정은 폴더 구조가 잡혀 있어야 값이 나오니까요. 이 순서로 갈까요?"

**"병렬로 출발"이 "병렬로 완주"를 뜻하지 않는다** — 출발은 같이 하되 특정 부분만 앞 작업을 기다리는 형태가 가능하다. 이 구분을 놓치면 "동시에 못 하니 아예 순차로"라고 과하게 직렬화하게 된다.

전 페이지를 훑어야 나오는 판정에 걸리는 PR도 같은 방식으로 제안한다 — 예를 들어 공통 컴포넌트 판정은 MARKUP이 전 페이지를 본 뒤에 나오므로([conventions/session/markup/index.md](../conventions/session/markup/index.md)), 그 판정에 영향받는 PR을 MARKUP 진행 중에 확정·출발시키려 하면 그 사실을 먼저 알린다.

---

## BG 세션 종료

step-1.2 종료 = **BG 세션 종료**. 남은 PR 확정이 있으면 마치고, SKILL.md 「세션 spawn 안내 메커니즘」으로 후속 spawn 안내를 출력한다.
