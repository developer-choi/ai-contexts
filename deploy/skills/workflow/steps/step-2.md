# Step 2: PR 분할 전략 수립

> **이 단계의 목표: PR 분할 전략을 수립한다**

여러 PR로 나뉠 수 있는 큰 작업의 분할 전략을 수립합니다.

---

## 작업 크기 판단

- **작은 작업** (커밋 3개 이하): 이 단계 건너뛰고 다음 단계로
- **큰 작업** (커밋 4개 이상, 또는 여러 도메인 포함): PR 분할 전략 수립

작업이 작다고 판단되면 사용자에게 스킵을 제안하세요.

---

## 채용과제: 고정 PR 구조

채용과제는 PR1~3이 고정이다. 구체적 항목은 Step 3에서 PR별로 정의한다.

| PR | 경계 |
|----|------|
| PR1 (프로젝트 세팅) | 빌드·린트·포맷 등 static checking 도구와 설정. 런타임 코드 없음 |
| PR2 (공통 인프라) | 런타임 의존성 설치 + Provider·API 클라이언트 등 앱 인프라 구성 |
| PR3 (공통 컴포넌트) | 2개 이상 페이지에서 쓰이는 재사용 UI 컴포넌트 |

**PR1~3은 분할 판단 대상이 아니다.** 분할 판단·codebase-audit는 PR4 이후에만 적용한다.

---

## 스킬 로드

아래 조건에 해당하는 스킬을 **모두** 로드합니다.

| 순서 | 조건 | 스킬 | 실행 | 설명 |
|:---:|------|------|------|------|
| 1 | 기존 코드를 건드리는 작업 | [codebase-audit](../codebase-audit/SKILL.md) | 서브에이전트 | 기존 코드 구조·패턴 파악 |

---

## PR 분할 원칙

- **한 번에 하나의 PR, 하나의 구체적인 기능**만 다룹니다.
- **의존성 순서**: 공통 모듈 → 개별 페이지
- **기술적 과제는 다음 단계에서** — 이 단계는 PR 분할에만 집중

---

## 기획 점검 (TODO 식별)

PR 분할 초안 작성 후, 각 PR별로 사용자에게 사전 확인이 필요한 TODO가 있는지 확인한다.

---

## 산출물: `/plan/project.md`

PR별로 아래 항목을 포함한다:
- PR 제목
- 범위
- 참고 background 파일
- 기획 확인 필요 사항 (TODO)

---

## 보고 내용

- 총 PR 수와 각 PR 범위 한 줄 요약
- PR 간 의존 순서
- 사용자 판단이 필요한 열린 질문 (있는 경우)

---

## 세션 회고

BACKGROUND_SESSION의 마지막 step이다. 보고 후:

1. `/pre-exit` 스킬을 호출하여 세션 마무리 절차를 수행한다.
2. 이 세션에서 사용자가 반복 수정 지시한 패턴이 있었으면 가이드 업데이트를 제안한다.
