# Codebase Audit (현황 분석 보고서)

## 1. 재사용 가능한 가용 자원

<ai-only>
새로 만들지 않고 재사용할 수 있는 기존 코드를 아래 카테고리별로 정리한다.

카테고리별 예시:
- UI 컴포넌트: `useModal` 활용
- 전역 상태/인프라: `AppContext`의 `locale`, `utils/api.ts`
- 유틸리티/상수: `src/constants/common.ts`
- 비즈니스 로직: `utils/format.ts`의 날짜 포맷팅
- 참고 코드: `UserSection.tsx`의 토글 로직
</ai-only>

- **UI 컴포넌트**: ...
- **전역 상태/인프라**: ...
- **유틸리티/상수**: ...
- **비즈니스 로직**: ...
- **참고 코드**: ...

---

## 2. 파일별 수정 및 생성 포인트

<ai-only>
어느 파일 어느 위치에 어떤 코드가 추가/수정되어야 하는지 구체적으로 기술한다.

- 수정 파일: 현재 상태(라인 범위 포함)와 수정 내용
- 신규 파일: 역할과 참고 레퍼런스
</ai-only>

### [수정] [파일명] (`path/to/file.tsx`)
- **현재 상태**: ...
- **수정 내용**: ...

### [신규] [파일명] (`path/to/new/file.tsx`)
- **역할**: ...
- **참고 레퍼런스**: ...

---

## 3. 중복 구현 방지 및 결론

<ai-only>
중복 구현을 방지하기 위한 분석 결론.
예: `MarketingModal` 새로 만들려 했으나, 기존 `ConfirmModal` 확장이 나음
</ai-only>