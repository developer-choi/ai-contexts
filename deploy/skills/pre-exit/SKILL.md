---
disable-model-invocation: true
name: pre-exit
description: 세션을 마무리한다. 이 세션에 해당하는 회고 항목을 먼저 보여주고 고른 것만 돌린 뒤, 미커밋 확인·커밋 정리·세션이 띄운 것 정리를 수행한다.
argument-hint: "(인자 없음 — 돌릴 회고 항목은 메뉴에서 골라 받는다)"
---

# 세션 마무리

## 목적

세션을 회고한다. 잘못된 것은 다음에 안 하고, 잘된 것은 다음에도 하게 만든다.

## 왜 필요한가

회고에서 건진 것은 대화와 함께 사라지고 다음 세션은 아무것도 모르는 채 시작한다. 그래서 세션이 닫히기 전에, 남겨야 할 것을 골라 규칙과 사례가 사는 자리에 심는다.

## 회고 항목

표의 순서가 실행 순서다. 보강과 시간·순서 표가 건진 것이 「문제 리스트업 + 규칙화」의 문제 목록으로 흘러 들어가므로 그쪽이 뒤에 온다.

| 항목 | 무엇을 하는가 | 상세 |
|---|---|---|
| 시간·순서 표 | 무엇이 오래 걸렸고 지시가 어떤 순서로 왔는지 한 표로 만들어 브라우저로 연다 | [timeline.md](timeline.md) |
| workflow 보강 | 안 따라간 cross-ref와 첫 요청을 `plan/pr{N}/**` 산출물에 맞대 누락을 건진다 | [augmentations/workflow.md](augmentations/workflow.md) |
| digest 보강 | 학습가치 keep/skip 판정이 번복된 자리를 모은다 | [augmentations/digest.md](augmentations/digest.md) |
| write-refine 보강 | 채점기를 돌리고 톤 교정 사례·놓친 결함·과교정을 회수한다 | [augmentations/write-refine.md](augmentations/write-refine.md) |
| routine 보강 | 루틴 데이터를 스크립트 우회로 다뤘는지, 그날 루틴을 굴린 방식 자체가 어땠는지 본다 | [augmentations/routine.md](augmentations/routine.md) |
| recruitment 보강 | 떠온 원문 중 안 쓰인 것과 뒤에서 다시 판 조사를 세어, 다음에 무엇을 떠올지 정한다 | [augmentations/recruitment.md](augmentations/recruitment.md) |
| 문제 리스트업 + 규칙화 | 세션에서 문제만 추출해 규칙과 사례가 사는 자리에 심는다 | [step-1.md](step-1.md) |
| 오답노트 (기본 꺼짐) | 몰라서 물어본 질문을 모아 남긴다. 바로 위 항목 안에서 돈다 | [error-notebook.md](error-notebook.md) |

## 무엇을 돌릴지 고른다

상세 파일을 열기 **전에** 고른다. 회고 항목에는 세션에 따라 아무 소득이 없는 것이 섞이는데, 상세를 먼저 열면 소득 없는 갈래의 절차까지 다 읽은 뒤에야 그걸 알게 된다.

1. `node {{skill_dir}}/scripts/session-state.mjs menu --session <session_id> --repo <레포 경로> --cwd <이 세션 cwd>`로 이 세션에 해당하는 항목을 받는다. 기억으로 답하지 않는다 — 회고는 세션이 길어진 뒤에 도는지라 앞머리가 요약으로 접혀 있기도 하고, 안 떠올라 항목을 건너뛰면 건너뛰었다는 것도 아무 데도 안 남는다
2. `해당`·`항상`·`미확인`으로 나온 항목만 위 표의 이름으로 번호를 매겨 한 번에 내고, **뺄 번호만** 회신받는다. `기본 꺼짐`은 따로 묶어 켤 번호를 함께 받는다. 항목마다 위 표의 「무엇을 하는가」를 한 줄로 붙인다 — 이름만 나열하면 무엇이 빠지는지 모르는 채 고르게 된다
3. 남은 항목의 상세 파일을 **그때** 연다. 안 고른 항목의 상세는 열지 않는다
4. 여러 개를 골랐으면 위 표의 순서로 돈다. 앞 항목이 건진 것은 「문제 리스트업 + 규칙화」의 문제 목록에 합류한다 — 순서를 뒤집으면 이미 만든 목록에 나중에 덧붙이게 된다

`해당없음`은 메뉴에 올리지 않는다. **다만 스크립트는 「불렀다」만 증명하고 「안 불렀다」는 증명하지 못한다** — 스킬이 자동 발동된 회차는 호출 기록이 안 생기므로, 그 스킬을 돌린 기억이 있으면 `해당없음`이어도 메뉴에 올린다.

`미확인`은 재료가 없어 못 본 것이라 `해당없음`과 다르게 처리한다. 그 항목은 메뉴에 올리고, 사유(transcript 없음·git 실패)와 스크립트가 이미 본 절반을 함께 적는다 — 고르는 쪽이 무엇을 모르는 채 고르는지 알아야 한다.

## 세션이 남긴 것 정리

회고에서 무엇을 골랐는지와 무관하게 이어서 돈다 — 이쪽은 골라 받지 않는다. 절차는 [cleanup.md](cleanup.md)를 따른다.

## 회고 항목을 새로 만들 때

[new-retro.md](new-retro.md)를 따른다. 회고를 도는 세션은 열지 않는다.
