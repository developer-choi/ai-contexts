---
description: 스킬 관련 필기를 수집·분류·정리하여 plan/backlog/에 쌓아둔다. skill-creator로 넘기기 전 준비 단계. 사용자가 스킬 아이디어를 언급하거나, 기존 스킬에 기능을 추가/개선하고 싶다고 말하거나, 스킬 관련 메모를 쏟아내면 반드시 이 스킬을 사용한다. "스킬에 추가하면 좋겠는데", "스킬 아이디어", "이런 기능 넣고 싶어", "필기 정리", "백로그" 등 스킬 변경·개선·신규 아이디어가 조금이라도 언급되면 트리거한다.
argument-hint: [구두 필기 또는 메모 파일 경로]
---

# Skill Backlog

스킬 관련 필기를 수집·분류·정리하여 백로그로 쌓아두는 스킬. 뒤죽박죽 메모를 스킬 단위로 분류하고, 기존 백로그와 병합하고, 기존 스킬과의 관계를 식별한다. 개별 스킬의 설계·구현은 skill-creator가 담당한다.

---

## 준비

### 1. 기존 백로그 파악

`plan/backlog/` 하위 파일을 읽어 이미 정리된 백로그를 파악한다. 없으면 첫 정리로 간주한다.

### 2. 기존 스킬 파악

`deploy/skills/` 하위 SKILL.md들의 description을 읽어 현재 스킬 목록을 파악한다. 필기를 분류할 때 기존 스킬과의 관계를 식별하기 위함이다.

---

## 입력 수집

- **구두 덤프**: 사용자가 말하는 대로 전부 받는다. 사용자가 "끝", "다 했어" 등 종료 신호를 줄 때까지 계속 받는다. 중간에 정리하지 않고 다 받은 뒤 한 번에 정리한다.
- **텍스트 파일**: 경로를 받으면 읽는다.

---

## 정리

### 1. 스킬 단위 분류

입력된 필기를 스킬 단위로 묶는다. 하나의 필기가 여러 스킬에 걸치면 각각에 배분한다.

### 2. 기존 스킬 연결

각 항목을 아래 중 하나로 분류한다:

- **기존 스킬 개선**: `deploy/skills/`에 이미 있는 스킬의 개선. frontmatter `related-skill`에 경로를 명시한다.
- **신규 스킬**: 기존에 없는 새로운 스킬.

### 3. 기존 백로그와 병합

`plan/backlog/` 기존 파일과 대조한다:

- **중복**: 같은 내용이면 제거
- **추가**: 기존 항목에 새 내용이 붙으면 해당 섹션에 병합
- **신규**: 새 항목이면 추가

### 4. 구체화 수준 판단

- **구체화된 항목** (기능, 동기, 절차 등이 어느 정도 잡힌 것) -> 별도 `plan/backlog/{skill-name}.md` 파일
- **모호한 항목** (한두 줄 메모 수준) -> `plan/backlog/index.md`에 항목으로 추가
- **100줄 이상**: index.md 내 특정 항목이 100줄 이상이면 반드시 별도 파일로 분리한다
- 기존에 index.md에 있던 것이 구체화되면 별도 파일로 승격하고 index.md에서 제거

---

## 산출물 형식

### 개별 파일: `plan/backlog/{skill-name}.md`

```markdown
---
status: draft | concrete
related-skill: deploy/skills/pre-exit/  # 기존 스킬 개선이면
---

# {스킬명}

## 동기

(왜 필요한지)

## {기능 1}

- 세부 내용
- 세부 내용

## {기능 2}

- 세부 내용
```

기능 단위로 섹션을 나눈다. 각 섹션 안에서 세부 내용을 정리한다.

### 인덱스: `plan/backlog/index.md`

```markdown
# 미분류

## {항목 제목}
- 한두 줄 메모

## {항목 제목}
- 한두 줄 메모
```

---

## 보고

정리가 끝나면 사용자에게 변경 내역을 보고한다:

- 신규 파일 생성 목록
- 기존 파일 업데이트 목록 (무엇이 추가/변경됐는지)
- 기존 스킬 개선으로 연결된 항목
- index.md에서 별도 파일로 승격된 항목 (있으면)
