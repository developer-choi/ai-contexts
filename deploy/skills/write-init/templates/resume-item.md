<!--
이 파일은 메인 에이전트가 참고하는 템플릿이다.
이 코멘트 블록은 메타 가이드다. 메인은 참조용으로 읽되, 본문 출력에는 포함하지 않는다. `## 본문 시작 ↓` 아래 섹션만 본문으로 사용한다.

## 안내 문구

"이력서 경력기술서에 넣을 항목의 원본 주제와 근거 자료를 주세요. 제목과 문제/해결/성과의 대략적 재료(수치·방법·결과)만 있으면 됩니다. 톤·문장은 이후 write-refine에서 시안 3개를 뽑아 다듬습니다."

## length_target default

제목 1줄, 문제 1~2줄, 해결 1~2줄, 성과 1~2줄. 링크는 포함하지 않는다 (출처는 frontmatter `refs`에만).

## 구조 규칙

포맷·구조·톤 규칙은 [resume-guide.md](../../../contexts/writing-guide/resume-guide.md)를 따른다 (4줄 블록, 링크 본문 X, 수치는 성과에 모으기, 방법 vs 성과 분리, plain-text 렌더링, 명사형·한다체 톤).

## 시안 다양화 연결

write-refine이 이 패키지를 받으면 자동으로 "시안 다양화 모드"에 진입해 서로 대비되는 3축 시안을 뽑는다. 따라서 write-init에서 한 가지 표현으로 고정하지 말고, 재료(수치·방법·결과)를 열거하는 식으로 남겨도 된다.

## 시리즈 작업 주의

여러 항목(여러 경력기술서 주제)을 순차로 만들 때는 `refs`에 이전 확정 항목들의 경로를 누적한다. write-refine이 시안 간 재료 중복을 방지할 때 참조한다.

-->

## 본문 시작 ↓

## [주제 제목 — 수치나 결과를 드러내는 명사형 제목]

문제: [1~2줄. 이 작업 전 어떤 상태였는지, 왜 문제였는지. 원인-결과 연쇄는 피하고 핵심만.]

해결: [1~2줄. 어떤 방법·구조로 풀었는지. 하네스 엔지니어링·Streaming 등 방법 키워드가 여기 들어감.]

성과: [1~2줄. 수치(퍼센트·건수·시간·코드량)와 효과. 페이지·버튼 수·라인 수 등 구체 규모도 이 줄.]
