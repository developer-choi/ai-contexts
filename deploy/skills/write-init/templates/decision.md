<!--
이 파일은 메인 에이전트가 참고하는 템플릿이다.
이 코멘트 블록은 메타 가이드다. 메인은 참조용으로 읽되, 본문 출력에는 포함하지 않는다. `## 본문 시작 ↓` 아래 섹션만 본문으로 사용한다.

## 안내 문구

"이번 결정에서 비교할 구현 방법들을 적어줘 — 각 방법의 코드 스케치, 장점, 단점. 그리고 선택한 방법과 그 이유, 선택하지 않은 다른 방법들의 이유까지."

## 비교 구조 강제 규칙 — 뼈대 단계 검증

비교 구조 강제 규칙(방법 2개 이상, 각 방법 장점·단점, 결론에 비선택 이유)은 [decision-guide.md](../../../contexts/writing-guide/decision-guide.md)를 따른다.

## 필수·선택 섹션

필수: `# 방향성 정의 N회차`, `## 목표`, `## 구현 방법`, `## 결론`
선택(주제별 추가 허용):
- `## 왜 구현 방법이 중요한가` — 배경·제약·파급 효과. 현재 코드 스케치 포함 시 비교 구체화
- `## 검증` 또는 `## 요지 미리 보기` — 실제 동작 확인 or 핵심 메시지 미리 요약
- `## N차 소비자 관점` — 책임 레이어가 여러 층일 때 stakeholder별 시점

## length_target default

방법 2개 + 장단점 + 결론 단락 = 약 300-600단어. 맞춤 섹션 추가되면 그만큼 늘어남. frontmatter `length_target` 명시 시 우선.

## 파일명 규칙 — 산출물 저장

1차 소비자 프로젝트의 `plan/` 하위에 `<topic>-decisions-stepN.md` 형식. 같은 topic이 여러 회차로 이어질 수 있음 (step1, step2...). N은 회차와 1:1 매핑.

## key_message 작성 가이드

"선택된 방법 + 핵심 근거" 한 문장. 예:
- "기본 케이스에서 `<label>`로 감싸기 채택 — JS 0줄 선언적 위임이 `<div>` + `handleClick`보다 기본값 비용이 낮음"
- "복합 케이스에서도 `<label>` + `htmlFor` 채택 — 선언적 쌍 vs 명령적 JS 비대칭이 유지되어 label 쪽이 여전히 덜 번거로움"
-->

## 본문 시작 ↓

# 방향성 정의 [N]회차. [이 회차에서 결정할 주제]

## 목표

[이 결정으로 달성하려는 것. 스코프가 좁거나 회차 간 분담이 있으면 여기 명시 — 예: "이 회차는 기본 케이스만 다룬다. 복합 케이스는 N+1회차에서"]

## 왜 구현 방법이 중요한가

[배경·제약·이 선택이 이후에 미치는 영향. 현재 코드 스케치를 함께 보여주면 비교가 구체적. 필수 아님 — 생략 가능]

## 구현 방법

### 방법 1. [짧은 이름]

```
[코드 스케치]
```

**장점:**

- [핵심 요약 **볼드** + 상세 설명]

**단점:**

- [핵심 요약 **볼드** + 상세 설명]

### 방법 2. [짧은 이름]

```
[코드 스케치]
```

**장점:**

- [...]

**단점:**

- [...]

<!-- 필요 시 방법 3, 4 추가. 보조 비교는 "### 방법 N. (번외) [이름]" -->

## 결론

[선택한 방법 + 핵심 근거 한 단락]

[비선택 방법들의 이유 — 단락·서브섹션·bullet 자유. 결론만 단독으로 읽어도 왜 다른 방법이 탈락했는지 파악 가능해야 한다]

[필요 시 결정의 스코프·제약 명시 — 예: "이 결론은 X 케이스에 한정. Y 케이스는 다음 회차에서 이어 판단"]
