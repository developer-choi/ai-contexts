# 팀 에이전트 규칙

스킬에서 "팀 에이전트"를 스폰하라는 지시가 있으면, 반드시 아래 순서를 따른다:

1. **TeamCreate**로 팀을 생성한다 (이미 팀이 있으면 생략)
2. **Agent** 호출 시 `team_name` 파라미터를 넣어 팀 에이전트로 스폰한다

Agent를 `team_name` 없이 단독 호출하면 서브에이전트(일회성)가 되어 다라운드 SendMessage 소통이 불가능하다. "팀 에이전트"라는 표현을 서브에이전트로 대체하지 않는다.
