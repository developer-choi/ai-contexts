# 문장 교정 사례

AI가 작성한 문장과 사용자가 확정한 문장을 기록합니다.
같은 실수를 반복하지 않기 위한 참고 자료입니다.

---

## 사례 1: "덜어주는 UX"

**Bad (AI)**

```
마우스 클릭이나 모바일 터치 한 번을 줄여 사용자의 불편함을 덜어주는 UX입니다.
```

```
페이지에 진입하자마자 바로 타이핑할 수 있는 것이 좋은 UX라고 생각합니다.
```

- "불편함을 덜어주는 UX" — 의미가 모호하고 주어-목적어 관계가 어색함
- "좋은 UX라고 생각합니다" — 뭘 말하려는 건지 전달이 안 됨

**Result (사용자 확정)**

```
이런 작은 불편함을 줄여주는 것이 좋은 UX 중 하나라고 생각합니다.
```

- "불편함을 줄여주는 것" — 동작이 명확
- "좋은 UX 중 하나" — 단정이 아닌 의견으로 톤 조절

---

## 사례 2: 문서 성격에 맞는 제목

**배경**: 폼 에러 피드백 예제 페이지. 기술 레퍼런스가 아니라 UX 관점에서 "왜 이렇게 해야 하는가"를 설명하는 에세이 형태의 문서.

**Bad (AI)**

```
1. Validation Mode
```

- 기술 레퍼런스/API 문서라면 기능명 제목이 맞음
- 하지만 이 문서는 독자에게 UX 판단 근거를 설명하는 에세이 — 기능명을 나열하면 매뉴얼이 됨

**Result (사용자 확정)**

```
1. 유효성검증은 언제 해야 하는가?
```

- 독자의 질문을 제목으로 → 읽는 목적과 일치
- 문서 성격(에세이/설명문)에 맞는 질문형 제목

---

## 사례 3: 목적 먼저, 구현 나중에

**Bad (AI)**

```
구조: mode 옵션 설명 → 코드 → 데모 → "왜 onSubmit인가"
소개: "react-hook-form의 useForm()은 mode 옵션으로 에러 노출 시점을 결정합니다."
라벨: onChange, onBlur, onSubmit
```

- 라이브러리 기능 설명부터 시작 → "뭘 왜 하는 건지" 파악 전에 "어떻게"가 먼저 나옴
- 기술 용어(onChange 등)로 라벨링 → 목적이 아닌 구현 관점

**Result (사용자 확정)**

```
구조: UX 관점(에러 시점 3가지) → 라이브러리 해결법 → 데모 → 코드 → 한계
소개: "에러를 보여주는 시점은 크게 세 가지입니다."
라벨: 입력 시점, 포커스가 빠진 시점, 제출 시점
```

- 목적(언제 에러를 보여줄 것인가)을 먼저 설명하고, 구현(mode 옵션)은 다음에
- UX 관점의 라벨 → 기술 용어 없이도 의미 전달

---

## 사례 4: 독자가 추론할 수 있는 결론 삭제

**Bad (AI)**

```
제출 버튼을 누르는 건 사용자 스스로 "다 채웠다"고 판단한 시점입니다.
이때 에러를 보여주는 게 가장 자연스럽습니다.
```

```
정답은 없지만, 라이브러리가 기본값을 onSubmit으로 설정한 데는 동의합니다.
대부분의 폼에서 가장 자연스러운 선택입니다.
```

- "다 채웠다고 판단한 시점"이라고 설명했으면 "자연스럽다"는 결론은 독자가 스스로 내릴 수 있음
- AI가 결론까지 달아주면 설교조가 됨
- "대부분의 폼에서 가장 자연스러운 선택입니다" — 직전에 "정답은 없지만"이라고 했으면서 "가장"이라고 단정하면 모순

**Result (사용자 확정)**

각각 해당 문장 삭제.

- 설명이 충분하면 결론은 독자의 몫
- "가장" 같은 단정은 "정답은 없지만"과 모순

---

## 사례 5: 한계 분석 후 개인 의견 추가

**Bad (AI)**

```
onBlur/onChange는 필드를 하나하나 채워 넘어가므로 이 문제가 원천적으로 없습니다.
(끝 — 개인 의견 없이 분석만으로 마무리)
```

- 한계를 지적만 하고 끝나면 "그래서 어쩌라고?"가 됨
- 필자의 입장이 빠져 있음

**Result (사용자 확정)**

```
onBlur/onChange는 필드를 하나하나 채워 넘어가므로 이 문제가 원천적으로 없습니다.
정답은 없지만, 라이브러리가 기본값을 onSubmit으로 설정한 데는 동의합니다.
```

- 분석 → 개인 의견으로 마무리
- "동의합니다" — 단정이 아닌 개인 입장

---

## 사례 6: 명령조 → 개인 의견

**Bad (AI)**

```
isValid로 버튼을 비활성화하면 사용자가 뭘 고쳐야 하는지 알 수 없습니다.
항상 활성화하고, 제출 시 에러 피드백으로 안내하세요.
```

- "안내하세요" — 명령조. 필자가 독자에게 지시하는 톤
- "isValid로" — UX 설명인데 기술 용어가 먼저

**Result (사용자 확정)**

```
제출 버튼을 조건이 안 맞다고 비활성화시켜 버리면 사용자는 뭘 고쳐야 하는지 알 수 없습니다.
실제로 부모님 세대에서 "제출 버튼이 왜 안 눌리냐"고 문의를 주시는 경우가 있습니다.
버튼은 항상 활성화하고, 제출 시 에러 피드백으로 안내하는 게 좋다고 생각합니다.
```

- "비활성화시켜 버리면" — 사용자 관점의 표현
- "좋다고 생각합니다" — 명령이 아닌 개인 의견
- 실제 경험 근거 추가

---

## 사례 7: 불필요한 디테일/요소 삭제

**Bad (AI)**

```
react-hook-form은 제출 시 첫 번째 에러 필드로 자동 포커스하고
```

```
추천 조합: mode: 'onSubmit' + reValidateMode: 'onChange' (둘 다 기본값).
제출 전까지 에러를 보여주지 않아 편안한 입력 경험을 제공하되,
에러 발생 후에는 수정할 때마다 즉시 피드백합니다.
```

```
// MODE_CODE 10줄+: mode, reValidateMode, 옵션별 동작 주석 전부 나열
```

**Result (사용자 확정)**

```
react-hook-form은 제출 시 에러 필드로 자동 포커스하고
```

```
(추천 조합 Callout 삭제)
```

```
// MODE_CODE 3줄
const { register, handleSubmit } = useForm({
  mode: 'onSubmit', // 'onSubmit' | 'onBlur' | 'onChange'
});
```

- "첫 번째" — 불필요한 디테일. 핵심은 "에러 필드로 포커스"
- 추천 조합 Callout — 각 섹션에서 이미 설명한 내용의 반복
- 코드 스니펫 — 독자가 알아야 할 핵심만 남기고 나머지 삭제

---

## 사례 8: 비개발자 독자 용어 순화

**Bad (AI)**

```
Convention Reviewer만 N명이 병렬로 리뷰하고, 나머지 단계 간은 순차입니다.
```

**Good (사용자 확정)**

```
컨벤션 검증만 여러 리뷰어가 동시에 수행하고, 나머지 단계는 순서대로 진행됩니다.
```

- "N명", "병렬", "순차"는 개발자에게는 명확하지만 비개발자에게는 의미 불분명
- 같은 뜻을 일상 용어로 바꾸면 정보 손실 없이 접근성이 올라감

---

## 사례 9: 구현에 따라 달라지는 것을 단정

**Bad (AI)**

```
하지만 TextAreaField는 `<textarea>`, SelectField는 `<button>`이 필요해서
```

- SelectField의 내부 요소가 `<button>`이어야 한다고 단정
- 실제로는 라이브러리에 따라 `<button>`일 수도 `<input>`일 수도 있음
- 같은 문서의 다른 곳에서 "라이브러리마다 다름"이라고 적어놨으므로 모순

**Result (사용자 확정)**

```
하지만 TextAreaField는 `<textarea>`가 필요하고, SelectField도 구현에 따라 `<button>` 등 `<input>`이 아닌 요소가 들어갈 수 있어서
```

- "필요하다"를 "들어갈 수 있다"로 변경 — 단정이 아닌 가능성
- 문서 내 다른 서술과 모순 없음

---

## 사례 10: 독자가 이해할 수 없는 내부 구조 서술

**Bad (AI)**

```
개별 props를 명시적으로 내부 요소에 전달 + `{...inputProps}` bag으로 추가 props를 후순위 spread
```

```
`Select.js` — `React.cloneElement`로 `OutlinedInput`에 `inputComponent={SelectInput}` 주입
```

- 특정 라이브러리(MUI)의 내부 구조를 알아야만 이해할 수 있는 문장
- 독자가 MUI 소스를 읽어본 적이 없으면 의미 전달 불가

**Result (사용자 확정)**

해당 섹션 전체 삭제.

- 선택지 A/B의 코드 예시와 장단점만으로 결정 근거가 충분했음
- 이미 설득된 논점에 외부 라이브러리 권위를 빌리는 것은 불필요한 디테일 (사례 7과 동일 패턴)

---

## 사례 11: 과장된 비유 표현

**Bad (AI)**

```
props 폭발 — `placeholder`, `autoComplete`, `type` 등 `<input>` 속성을 InputBase가 중계해야 함
```

- "폭발"은 과장된 비유로, 구체적으로 무엇을 뜻하는지 독자마다 다르게 해석
- 문서 전체가 차분한 한다체인데 이 부분만 과장 표현

**Result (사용자 확정)**

```
props가 많아짐 — `placeholder`, `autoComplete`, `type` 등 `<input>` 속성을 InputBase가 중계해야 함
```

- 과장 없이 사실 그대로 서술
- 문서 톤과 일관

---

## 사례 12: 내용보다 양식이 먼저

**배경**: AI가 문서 내용 리뷰를 먼저 진행하고, 여러 라운드의 수정을 거친 뒤 사용자가 양식(구조/제목 체계)을 변경하여 작업이 낭비됨.

**Bad (AI)**

```
1. 내용 리뷰 (톤, 전방 참조, 라인번호 등 여러 라운드)
2. 양식 논의 (사용자가 "당연히 처음부터지"라고 지적)
3. 양식 확정 후 재작성
```

- 양식이 바뀌면 내용 배치도 바뀌므로, 양식 확정 전 내용 리뷰는 낭비
- 사용자가 지적하기 전까지 순서 문제를 인식하지 못함

**Result (사용자 확정)**

```
1. 양식 확정 (섹션 순서, 제목 체계, 래핑 구조)
2. 내용 작성/리뷰
```

- 양식을 먼저 확정하고 내용을 채움
- 확정한 양식을 전달할 때 제목 체계, 래핑 구조 등 형식 요소도 명시

---

## 사례 13: 핵심 메시지 파악 없이 형식 리뷰에 진입

**배경**: Turbopack/Webpack 환경 차이 대응 문서. 사용자의 핵심 메시지는 "silent failure가 근본 문제"였으나, AI는 이를 파악하지 못하고 톤·중복·부록 분리 같은 형식 리뷰부터 진행. 사용자가 직접 양식을 재배치한 뒤에야 따라감.

**Bad (AI)**

```
1. 톤 리뷰 (습니다체, 평서문 나열 등 형식 이슈 5건 제출)
2. 구조 제안 ("알려진 차이"를 부록으로 분리)
3. 사용자가 "내가 집중하고 싶은 건 silent failure 그 자체"라고 지적
4. 사용자가 직접 양식 재배치 (기존 배경 → 최초 발견 → 근본 문제 → 집중할 문제)
5. AI가 뒤따라 말투만 정리
```

- 문서가 독자에게 전달하려는 핵심 메시지를 파악하지 않고 형식부터 리뷰
- 사례 12("내용보다 양식이 먼저")의 상위 패턴: 양식조차도 핵심 메시지를 먼저 파악해야 제안할 수 있음
- 결과적으로 AI의 구조 제안(부록 분리 등)이 사용자의 최종 양식과 달라 작업이 낭비됨

**Result (사용자 확정)**

```
1. 핵심 메시지 파악 ("이 문서가 독자에게 뭘 전달하려는가")
2. 핵심 메시지에 맞는 양식 제안 (섹션 순서, 제목 체계)
3. 양식 확정 후 톤·분량 리뷰
```

- 핵심 메시지를 모르면 양식도 제안할 수 없음
- 사례 12의 "양식 먼저"를 한 단계 더 확장: 양식 전에 핵심 메시지가 먼저

---

## 사례 14: 결론부 톤 불일치 — 단답 결정 vs 분석 톤

**배경**: ADR 문서. 본문에서 장단점을 분석하는 톤으로 진행했는데, 결론만 일방적 결정 톤으로 마무리.

**Bad (AI)**

```
**방법 2(children)로 진행합니다.**
```

- 본문 전체는 "~할 수 있습니다", "~가 필요해집니다" 같은 분석 톤
- 결론만 "진행합니다"로 단답 결정 — 어감이 끊김
- 필자가 일방적으로 결정을 내려버리는 톤

**Result (사용자 확정)**

```
확장성과 API 단순함을 고려하면 **방법 2(children)가 적합하다고 판단했습니다.**
```

- 판단 근거를 한 줄 깔고 결정
- "판단했습니다" — 단정이 아닌 개인 의견 톤
- 본문의 분석 톤과 일관

---

## 사례 15: 외부 라이브러리 근거 인용 시 실제 구조 확인

**배경**: InputBase의 children 주입 방식(방법 2)의 근거로 Radix SelectTrigger 코드를 인용. 하지만 Radix Select는 베이스 컴포넌트를 공유하는 구조가 아니라 아예 독립 구현이므로, "children 주입"의 근거가 아님.

**Bad (AI)**

```
Radix도 Select 계열에서는 이 방향을 택하고 있습니다. SelectTrigger는 TextField와 공유하는 컴포넌트가 하나도 없고, 내부 폼 요소로 `<button>`을 사용합니다:
(SelectTrigger 코드)
```

- Radix Select는 TextField와 베이스를 공유하지 않으므로, "이 방향을 택했다"는 근거가 성립하지 않음
- 구조 자체가 다른 코드를 같은 선택지의 근거로 인용하면 오해를 줌

**Result (사용자 확정)**

해당 문단 삭제. 대신 방법 4(번외)로 Radix의 독립 구현 방식을 별도 섹션에 소개.

- 외부 라이브러리를 근거로 인용할 때, 그 라이브러리가 실제로 해당 방법을 쓰는지 확인
- 구조가 다른 경우 근거가 아니라 별도 방식으로 분류

---

## 사례 16: ~화 용어 순화

**Bad (AI)**

```
에러 처리 이원화 설계: Error 클래스(개발자/Sentry용) + getErrorInfo(사용자 노출용) 분리
```

**Result (사용자 확정)**

```
커스텀 에러 클래스 도입으로 에러 발생 시 개발자와 사용자 각각에게 필요한 정보를 전달
```

- "이원화"는 독자에게 생소한 한자어. 구체적으로 풀어씁니다.
- ~화 계열 한자어는 쉬운 동사·명사로 대체합니다. (tone.md 11번 참고)

---

## 사례 17: 목적 먼저, 수단 나중 (섹션 순서)

**Bad (AI)**

```
섹션 순서: 에러 클래스(수단) → 에러 문구(UX) → 에러 잡는 구조(목적)
```

**Result (사용자 확정)**

```
섹션 순서: 에러 잡는 구조(목적) → 에러 문구(UX) → 에러 클래스(수단)
```

- 독자는 "왜"를 먼저 알아야 "어떻게"에 납득합니다.
- 수단·구현 세부사항이 먼저 나오면 목적을 파악하기 전에 피로해집니다.
- 섹션 내 문장 순서뿐 아니라 섹션 배치 순서도 이 원칙을 따릅니다.

---

## 사례 18: 삭제 섹션 부활

**상황**: 사용자가 초안에서 "배경", "AppProvider", "sass+디자인토큰" 섹션을 삭제한 뒤 피드백을 남김. 메인이 재작성하면서 삭제된 섹션을 다시 추가함.

**Bad (AI)**

```
재작성 시 사용자가 삭제한 섹션("배경", "AppProvider", "sass+디자인토큰")을 다시 포함
```

**Result (사용자 확정)**

```
삭제한 섹션은 재작성에서도 제외
```

- 사용자가 삭제한 섹션은 "이 내용이 불필요하다"는 명시적 의사입니다.
- 재작성 시 삭제된 섹션을 부활시키지 않습니다.
