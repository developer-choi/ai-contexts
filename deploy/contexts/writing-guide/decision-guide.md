---
tags: [decision]
---

# decision-guide

기술적 의사결정 문서(`type: decision`) 전용 작성·검토 규칙입니다. `write-init`에서 `type: decision`으로 입력받아 `write-refine`이 다듬을 때 함께 로드됩니다.

여러 구현 방법을 비교하고 선택 근거를 남기는 문서입니다. 결론만 단독으로 읽어도 "왜 다른 방법이 탈락했는지" 파악되어야 합니다.

## 비교 구조 강제 규칙

뼈대 단계와 다듬기 단계 모두에서 아래를 검증합니다. 위반 시 보강을 요청합니다.

- **방법 2개 이상.** 주력 둘에 더해 `(번외)` 라벨을 단 보조 비교도 개수에 포함합니다.
- **각 방법에 장점·단점 둘 다 채움.** 한쪽만 있거나 공란이면 보강 요청.
- 위 둘은 `score.mjs`가 결정론으로 잡습니다. 헤딩·라벨의 정확한 표기는 그 검사기가 정본이라 여기에 옮겨 적지 않습니다 — 표기가 틀리면 검사기가 그 자리에서 알립니다.
- **결론에 선택 이유와 비선택 이유가 모두 드러남.** 형식은 자유(단락·서브섹션·bullet). 결론 섹션만 단독으로 읽어도 "왜 다른 방법이 탈락했는지" 파악되어야 합니다. "방법 N 선택" 명시만 있고 탈락 사유가 본문·서브섹션·bullet 어디에도 없으면 보강 요청.
- **사용자가 직접 채움.** AI는 장단점·탈락 사유 블록을 임의로 채우지 않습니다. 비어 있으면 사용자에게 항목별로 질문해서 받아 적습니다. 사용자가 명시적으로 "AI가 초안 채워라"고 요청한 경우에만 AI가 채우되, 사용자 검토·승인을 받아야 확정합니다.

## 서술 규칙

트레이드오프를 적는 방식의 규칙(감수한 단점 정당화 금지 / 비선택 비용은 기능으로 적고 구현 단정 금지)은 [tradeoff-guide.md](tradeoff-guide.md) 「서술 규칙」에 있습니다. decision 전용이 아니라 트레이드오프를 적는 모든 기술 문서에 적용되므로 그쪽을 SSOT로 둡니다. decision 문서를 작성·다듬을 때 함께 로드해 적용합니다.
