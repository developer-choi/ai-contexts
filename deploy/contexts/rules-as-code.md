# 코드로 표현 가능한 규칙은 코드로

## 핵심

규칙을 "지키도록 부탁"하는 방식(프롬프트·메모리)은 매 턴 LLM의 준수에 의존해 확률적이고, 어겨도 아무도 안 잡는다. 어기는 게 구조적으로 불가능하거나 번거롭게 만드는 쪽으로, 가능한 만큼 항상 왼쪽으로 민다. 규율(discipline)에서 설계(design)로.

## 강제 사다리

```
없앤다(codegen·SSOT)  >  불가능하게(타입·제약)  >  강제(정적분석·hook)  >  부탁(프롬프트)
```

밑에 깔린 축은 **보장 강도**다. 왼쪽일수록 강하다.

- **없앤다** — 한쪽을 다른 쪽에서 생성하면 어긋날 짝이 사라진다 (codegen·단일 출처).
- **불가능하게** — 타입·권한·제약으로 위반 자체가 표현 불가능해진다.
- **강제** — 정적분석·hook·lint이 위반을 결정론적으로 잡는다. 같은 강제 칸 안에서도 **발동 범위**로 우선순위가 갈린다: 정적분석·lint(eslint·tsconfig·stylelint)은 사람·CI·타 도구 어디서나 걸리고, 에이전트 hook은 그 hook을 배포받은 에이전트 안에서만 발동한다 → 코드 규칙은 정적분석이 먼저, hook은 그다음. (에이전트 행동·채팅 인터셉트는 정적분석이 적용 불가하므로 hook이 그 도메인에서 가장 강한 수단.)
- **부탁** — 프롬프트로 적어두고 매 턴 준수를 바란다. 가장 약하다.

칸 이름(정적분석·hook…)은 수단의 예시일 뿐이다. validate 린터처럼 "이름엔 없지만 더 강한 보장을 주는" 수단은 강도 축으로 판정해 제 칸에 둔다.

## 내릴 수 있는가 — 단일 게이트

규칙을 한 칸 왼쪽으로 내릴 수 있는 기준은 하나다.

> **LLM 판단이 1도 안 남게 완전히 코드로 표현되는가.**

판단이 조금이라도 남으면 못 내린다. 0이면 내린다. 비용(작성·유지·오탐)은 별도 기준이 아니다 — 코드로 내리면 어차피 더 안전하고, 유지비는 프롬프트로 둬도 든다. "지금 짤 시간이 있나"는 일반 작업 우선순위지 이 게이트가 아니다.

### "코드로 표현 가능"은 거의 항상 과장된다

규칙의 기계적인 부분만 보고 판단이 남는 부분을 빼먹는 함정이 흔하다. 판단이 일부라도 남으면 게이트 통과 실패다.

### hook은 발동만 보장한다

판단 기반 행동(맞춤법·표현 교정 등)은 기계 판정이 불가능하다. 이런 규칙에 hook을 걸어도 hook은 *발동*(그 순간 LLM을 깨우는 것)만 보장하고, 실제 동작은 여전히 LLM이 판단해 수행한다. 강제 칸처럼 보이지만 보장 강도는 부탁에 가깝다 — 이 한계를 알고 선택한다.

## 없앤다 칸의 경계 — 파생 가능성

`없앤다` 칸은 **B가 A에서 파생 가능할 때만** 열린다.

- what/how 주석 = 코드에서 파생 가능 → 없앤다 ✅
- why 주석(의도·근거·외부 제약·판단) = 파생 불가능 → 못 없앤다. 한 칸 내려와 강제·부탁 ❌
- 짝꿍 동기화: "어떤 파일이 짝인가(사실)"는 데이터로 내리고, "제대로 고쳤나(판단)"는 사람 몫.

## 정본이 코드에 있으면 산문은 옮겨 적지 않는다

사다리가 "규칙을 코드로 내려라"라면 이 절은 내린 다음의 규율이다. 목록·설정·명세가 코드에 정본으로 있으면 산문에 같은 것을 적지 않는다. 적는 순간 두 벌이 되고, 코드만 바뀌면 산문은 조용히 거짓이 된다.

- **판정 질문: 이 목록을 사람이 손으로 고치나.** 아니오면 코드가 정본이므로 산문의 복사본을 지운다.
- **사본을 지키는 감시 장치를 만들고 싶어지면 그것이 사본을 지우라는 신호다.** 어긋날까 봐 대조 검사를 새로 만드는 순간, 없어도 될 사본을 지키느라 검사 하나를 더 짊어진다. 그 검사는 대개 사본의 일부만 대조해 정작 바뀐 부분을 놓친다.
- **걷어낸 자리에 안내문도 남기지 않는다.** `package.json`·`eslint.config.*`·`tsconfig.json`처럼 도구가 정한 표준 위치는 독자가 이미 아는 자리라 "목록은 거기 있다"는 한 줄조차 사본의 잔재다. 표준 위치가 아닌 코드 내부(배열·상수)를 가리킬 때만 한 줄을 남긴다.
- **문서 제목에 「명세」·「스펙」을 붙이지 않는다.** 그 이름이 다음 사람에게 코드에서 읽히는 것을 다시 채워 넣게 만든다. 남길 것이 있으면 「코드에 없는 맥락」처럼 무엇이 남는지를 제목에 쓴다.
- **목록이 산문에 있어야 하면 방향을 뒤집는다.** 사람이 자주 손대고 근거·설명이 필요한 목록은 md를 정본으로 두고 코드가 그 md를 읽게 한다. 사본이 없으니 어긋날 수 없다.

실측 (2026-08-06, KA = `~/WebstormProjects/main/knowledge-archive`): 린터 체크 목록의 정본은 `scripts/validate-lint.mts`의 `CHECK_REGISTRY`인데 스킬 문서가 이를 표로 한 벌 더 적고, 그 표가 어긋날까 봐 표와 레지스트리를 대조하는 린터 체크를 새로 만들었다. 표는 4커밋 만에 26행에서 33행으로 자랐고, 대조 검사는 ID만 봐서 severity가 `warn`→`error`로 바뀐 것을 놓쳤다. 표와 검사를 함께 삭제(`26826f9`). 이어서 `CLAUDE.md`의 `package.json` scripts 표를 지우며 "목록은 `package.json`을 본다"는 안내문을 남겼다가 그 절도 삭제(`3253d24`). 코드 명세를 옮겨 적은 문서 한 편은 세 절을 지우고 제목을 「코드에 없는 맥락」으로 바꿨다(`ecff9a0`).

## 수단 목록 — 발동 순서로

| 수단 | 예시 | 언제 | 누구에게 | 한계 |
|---|---|---|---|---|
| codegen | AC `sync:*`(`deploy/`→`~/.claude/`, `CLAUDE.md`→`AGENTS.md`), PandaCSS `panda codegen` | 원본에서 생성할 때 | 전부 | 한쪽이 다른 쪽에서 파생 가능할 때만 |
| 타입·스키마 | `tsconfig.json` 컴파일 옵션, TS 타입, zod | 코드를 쓰는 중 | 전부 | 타입으로 표현되는 것만 |
| 에이전트 hook | AC `deploy/hooks/`의 PreToolUse 정책 검사 | 도구 호출 직전·직후 | 에이전트만 | 차단은 직전 이벤트만 되고, 명령 해석에 실패하면 아예 안 걸린다 |
| 정적분석 | eslint, stylelint, markdownlint, commitlint | 검사를 돌릴 때 | 전부 | 안 돌리면 아무 일도 안 일어난다 |
| git hook | 레포 `.githooks/pre-commit`, 전역 `hook.<이름>.command` | 커밋·푸시 | 전부, 이 컴퓨터 | 검사 생략 옵션으로 넘어간다 |

codegen만 성격이 다르다. 나머지는 위반을 *잡지만* codegen은 어긋날 짝 자체를 없앤다 — 그래서 사다리에서도 가장 강한 「없앤다」 칸이다. 쓸 수 있으면 아래 줄들을 볼 일이 없어진다.

고를 때:

- 위반을 만드는 주체가 에이전트뿐이면 에이전트 hook, 사람·다른 도구까지면 정적분석·git hook.
- 정적분석은 혼자 두면 발동 자체가 안 걸리므로 git hook에 태운다. 무엇을 볼지는 정적분석이, 언제 반드시 돌지는 git hook이 맡는 조합이다.
- git hook으로 정했으면 전역과 그 레포 `.githooks/` 중 어디에 둘지가 다시 갈린다. **검사기가 보는 대상이 특정 레포를 가리는가**로 정한다 — 가리면 그 레포, 안 가리면 전역. 전역 훅은 어느 레포에서든 도는 자리라(그래서 훅을 만든 레포의 모듈조차 import하지 못한다) 레포 전용 검사기를 얹으면 다른 모든 레포의 매 커밋이 없는 경로를 찾다 끝나고, 거꾸로 레포에 둔 범용 검사기는 다른 레포에서 아예 안 걸린다. 어느 쪽도 실패 신호를 내지 않아 조용히 굴러간다.
  - 레포에 두기로 했으면 등록 명령을 `|| true`로 감싼다. 그 훅 파일이 없던 과거 커밋을 rebase·cherry-pick으로 다시 만들 때 실행 실패가 그대로 커밋 실패가 되어, 되묻기만 하는 훅이 작업을 막는다. 전역 훅은 등록 자리가 레포 히스토리 밖이라 이 함정이 없다.

## 검사 범위 — 고친 줄이 아니라 건드린 파일

변경분만 보는 검사는 만들기 쉽지만, 규칙을 만들기 전부터 있던 위반은 그 줄을 직접 건드리기 전까지 남는다. 그렇다고 규칙을 새로 만들 때마다 전체를 훑어 정리하면 규칙 수만큼 비용이 곱해져 아무도 안 하게 된다.

**이 커밋이 건드린 파일을 통째로 본다.** 이미 그 파일을 열어놓은 자리라 고치는 비용이 가장 싸고, 옛 위반이 손대는 김에 하나씩 걷히면서, 규칙이 늘어도 커밋당 비용은 안 는다.
