# Client/Server Component 페이지네이션 추가

onClick / URL 두 방식 모두 지원하고, Client Component·Server Component 양쪽에서 쓸 수 있는 페이지네이션을 추가한다.

- onClick 방식: 상태 기반 페이지 전환 (Client Component)
- URL 방식: `href` 생성 규칙 기반 (Server Component에서도 동작)
