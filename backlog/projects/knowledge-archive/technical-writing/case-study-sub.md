# 기술 케이스 스터디 — 하위 양식 시안 (시리즈 한 편)

<!--
용도: 시리즈 한 편. 한 목표를 깊이 다룬다.
원칙: 6섹션 양식. 후보 비교(3번)는 여기에 자세히, 루트에는 한 줄씩만.
운영 룰:
- 다른 편과 패턴이 겹치면: 한 글이 canonical(원전), 다른 글은 짧게 + 링크.
- 코드 스니펫은 강조점이 다르면 자연스럽게 갈라짐. 메커니즘 정의 자체는 원전 한 곳.
예시 채움: Sentry 도입기 1편 — 누락 없이 수집 (G1).
-->

---

> **Sentry 도입기 시리즈**
> - [개요] 86종 이슈를 4개 목표로 정리한 기록
> - **[1편] 모든 에러를 누락 없이 수집** ← 현재
> - [2편] 경중 구분 + 긴급 알림
> - [3편] 풍부한 단서 자동 첨부 (패턴 원전)
> - [4편] Sentry 모르는 동료도 유지보수

# Sentry 도입기 1편 — 모든 에러를 누락 없이 수집

## 1. 문제 발견

<!-- 사례 전체 Before에서 이 목표 부분을 좁힘. 근본 원인 명시. -->

캐치되지 않은 에러로 사용자가 직접 제보할 때까지 개발팀이 인지하지 못했다.
실제로 구매 페이지가 깨진 사례에서 약 2,000만 원 규모의 손실 발생.

**근본 원인**: 프론트엔드 에러를 자동 수집하는 모니터링 시스템 부재. 페이지·핸들러마다 수집 호출이 일관되지 않았고, 사용자 제보가 유일한 인지 경로였음.

## 2. 목표 정의

<!--
문제를 완전히 해결하려면 결과적으로 어떻게 동작해야 하는가.
이 글은 단 하나의 목표만 다룬다. 다른 목표는 시리즈의 다른 글로.
-->

에러가 발생할 수 있는 모든 지점에서 누락 없이 수집된다.

## 3. 해결방법 리스트업

<!--
이 목표를 달성할 수 있는 후보 N개 + 각각의 trade-off.
trade-off 분류: 성능 / 복잡도 / 유지보수 비용 / 학습 비용 / 운영 부담 / 확장성 등.
-->

### A. 각 핸들러마다 try-catch + captureException 수동 호출

- 장점: 발생 지점 컨텍스트가 명확
- Trade-off:
  - 보일러플레이트 (복잡도·유지보수 비용)
  - 호출 누락 위험 (목표의 정반대)
  - 동료가 SDK 학습 필요 (학습 비용)

### B. Interceptor 패턴 (요청·응답 가로채기)

- 장점: 한 곳에서 관리
- Trade-off:
  - API 외 에러는 못 잡음 (확장성)
  - UI 렌더 에러·비동기 콜백 에러 누락

### C. 글로벌 핸들러 + ErrorBoundary

`window.onerror`, `unhandledrejection`로 캐치 안 된 에러 일괄 수집. React 렌더 에러는 `ErrorBoundary`.

- 장점: 모든 캐치 안 된 에러 자동 포착
- Trade-off:
  - 발생 지점의 의미적 컨텍스트가 부족 → 단서 보강이 별도로 필요

## 4. 선택 및 근거

<!--
어느 옵션을 택했고 왜. "남들이 많이 써서"는 근거 아님.
현재 프로젝트의 상황(스택, 팀, 우선순위)에 비추어 왜 최선이었는가.
-->

C 선택. 누락 0이 이 목표의 절대 조건이라 A의 누락 위험, B의 커버리지 부족은 즉시 탈락.
C의 단서 부족 문제는 [3편] 패턴으로 보완.

## 5. 구현 과정

<!-- 어떻게 코드로 옮겼나. 핵심 메커니즘 + 엣지 케이스. -->

- `window.addEventListener('error', ...)` + `window.addEventListener('unhandledrejection', ...)`
- React 트리 최상위 `ErrorBoundary` 컴포넌트
- 의도적 throw 지점은 그대로 두고 글로벌 핸들러가 받음

## 6. 성과

<!-- 이 목표 달성 여부 + 정량/정성 결과 + 남은 한계. -->

- 누락 0: 어디서 에러가 나든 자동 수집됨
- 신규 페이지·버튼 추가 시 별도 수집 코드 불필요
- 남은 한계: 발생 지점의 의미적 컨텍스트는 부족 → [3편] 패턴으로 보완
